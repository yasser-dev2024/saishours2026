# تقرير الاختبارات

تاريخ التنفيذ: 2026-08-03.

## النتائج النهائية

| الفحص | النتيجة |
|---|---|
| `flutter analyze` | ناجح — `No issues found` |
| `flutter test --concurrency=1` | ناجح — 15 اختبار Unit/Widget |
| `integration_test/app_test.dart` على اللوحي | ناجح — التنقل، عدم وجود مفتاح، الجرس، مركز التنبيهات، ملف الخيل والكاميرا |
| `integration_test/data_workflows_test.dart` على اللوحي | ناجح — دورة بيانات كاملة ونسخ واستعادة وتقارير شاملة وقواعد الميزانية والدين وبقاء البيانات |
| SQLite integrity للأصل/النسخة/الأصل المضمن | `ok` للثلاثة |
| تثبيت وتشغيل Release | ناجح |
| صفحة التنزيل المحلية | ملف APK النهائي مطابق لنسخة Release؛ الحجم 61,354,580 بايت والبصمة موثقة |

## التغطية المنفذة

- تنظيف المدخلات والهاتف والتحقق من المبلغ والتاريخ والملف المزيف.
- إضافة خيل ثم تحديثه جزئيًا والبحث عنه.
- إضافة عضو واشتراك وربط الخيل وإنشاء رقم عضوية.
- تجديد الاشتراك وأرشفة الدورة السابقة.
- تسجيل دفعة ومصروف وتطعيم وموعد متأخر، والتحقق من التنبيه.
- إنشاء حجز وتوليد PDF والتحقق من ترويسة `%PDF`، وإنشاء تقريري العضو والخيل الشاملين متعددي الأقسام والصور.
- التحقق من الفصل بين المدفوع والمتبقي، والمصروف المؤثر وغير المؤثر في الميزانية، ومصروف مالك الخيل ومصروف النادي.
- إنشاء نسخة احتياطية وفحص SHA-256 وSQLite.
- إغلاق قاعدة البيانات وإعادة فتحها والتحقق من بقاء السجل.
- رفض نسخة تالفة، ثم استعادة النسخة الأصلية ومقارنة أعداد السجلات.
- عرض كل وجهات التنقل، جرس التنبيهات، زر تفعيل الإشعارات، وتبويبات العلاج والإيواء والتمارين.
- ربط نوع التنبيه وسببه بالقسم الدقيق، وتغيير هوية التنبيه عند تعديل التاريخ أو السبب مع استبدال إشعار النظام.
- التحقق من أن إخفاء التنبيه لا يستدعي حذف السجل الأصلي، لذلك يبقى ظاهرًا في ملف الخيل والخط الزمني والتقرير.
- تشغيل `jrs.mp3` من زر الاختبار؛ سجّل أندرويد `MediaPlayer` للحزمة في حالة `started` بخصائص `USAGE_ALARM` و`CONTENT_TYPE_SONIFICATION`.
- معاينة بطاقات المشتركين النهائية على اللوحي: ست حقائق أساسية مكتوبة بلا حقول فارغة، مع بطاقة تنبيه مدمجة عند الحاجة.

## عيوب اكتشفتها الاختبارات وتم إصلاحها

1. استُبدل استعلام `PRAGMA journal_mode` غير الصحيح باستعلام `rawQuery` المتوافق مع Android.
2. أُضيف مورد أيقونة الإشعارات الصحيح لمنع فشل التهيئة.
3. زيد ارتفاع بطاقات لوحة التحكم بعد كشف overflow مقداره 10 بكسل.
4. صُحح التحديث الجزئي وتجديد الاشتراك حتى لا يطلبا الاسم الموجود مسبقًا.
5. أضيفت أجزاء المللي ثانية لأسماء النسخ الاحتياطية لمنع تصادم نسختين في الثانية نفسها.
6. أضيف مشغّل صوت Android أصلي مع مشغّل Flutter احتياطي، وثُبت تشغيله من سجل النظام.
7. غُيّر حذف التنبيه إلى إخفاء التنبيه فقط، مع إبقاء السجل في ملف الخيل والتقرير.
8. أصبحت هوية التنبيه تشمل التاريخ والنوع والسبب، مع توافق الكتم القديم وعدم عودة التنبيهات المكتومة.
9. صُغّرت بطاقات المشتركين وأصبحت تعرض البيانات الفعلية بدل الشرائح الفارغة.

بيانات دورة التكامل كانت معزولة ومؤقتة. أما إصدار Release النهائي 1.2.2 فثُبّت كتحديث `adb install -r` بالشهادة المطابقة، فحافظ على بيانات الجهاز الفعلية (5 خيول و6 مشتركين) ولم تُضف أو تُحذف سجلات أثناء اختبار القبول النهائي.
