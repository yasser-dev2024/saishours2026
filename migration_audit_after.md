# جرد تطبيق سايس الخيل بعد التحويل

تاريخ الجرد: 2026-08-03  
المشروع: `horse_club_mobile`  
الحالة: تطبيق Flutter مستقل؛ لم تُعدّل ملفات برنامج سطح المكتب الأصلي.

## الملخص

- Flutter Stable 3.32.4، Dart 3.8.1، Material 3، واجهة عربية RTL.
- 8 وجهات تنقل رئيسية و14 ملف شاشة، إضافة إلى الحوارات والتبويبات والمحررات العامة.
- 39 ملف Dart ضمن `lib` والاختبارات، و75 تعريف class تقريبًا و172 دالة/method قابلة للرصد آليًا.
- 18 جدول أعمال SQLite محفوظة، مع migrations آمنة وإصدار قاعدة بيانات `3`.
- 19 تقريرًا فعليًا مع توليد PDF ومعاينة وطباعة ومشاركة.
- 16 أصلًا داخل حزمة التطبيق: قاعدة البيانات، الهوية، الخط، الصوت، العقود والتوقيعات.
- Android كامل بحزمة `com.abuammar.horseclub` وminSdk 23 وtarget/compileSdk 36 ودعم arm64-v8a وarmeabi-v7a وx86_64.

## الشاشات

### التنقل الرئيسي

1. الرئيسية ولوحة المؤشرات والتنبيهات.
2. الخيول.
3. المالية.
4. المشتركون.
5. الحجوزات اليومية.
6. السجل المالي.
7. التقارير.
8. الإعدادات.

### الشاشات والتفاصيل المساندة

- Splash أصلية مدتها الدنيا 2.2 ثانية من دون وميض أبيض أو أسود.
- مركز تنبيهات مستقل بفلاتر الكل والعاجلة والمواعيد والاشتراكات.
- ملف الخيل في 9 تبويبات: المعلومات، الصحة، العلاج، الإيواء والغرفة، التحذية، اليومية والتمارين، الخط الزمني، المواعيد، المصروفات.
- ملف العضو في 6 تبويبات: الملف، الاشتراكات، الدفعات، السجل المالي، الخيل، العقود.
- محرر عام مرتبط فعليًا بكل جدول، مع حالات تحميل وفراغ وخطأ ونجاح وتأكيد للحذف.
- شاشة تقارير ومعاينة/طباعة/مشاركة، وشاشة إعدادات للهوية والخدمات والنسخ الاحتياطي والإشعارات.

## قاعدة البيانات والترحيل

- قاعدة المصدر، نسخة ما قبل التحويل، ونسخة APK المضمنة لها SHA-256 واحد:
  `ACBD3B537A1D63FFCB80584B76C13B0ABDE3181A86438DBB846F730CA19D2229`.
- `PRAGMA integrity_check = ok` للملفات الثلاثة، وعدد جداول الأعمال 18 في كل منها.
- لم يُحذف جدول أو حقل أصلي. أضيفت حقول العضوية `member_code` و`is_vip`، وحقول صور إلى سجلات الصحة والمواعيد والمصروفات والمتابعة والحجوزات والإيواء والعلاج.
- migrations تستخدم `CREATE IF NOT EXISTS` و`ALTER TABLE` الشرطي، وتضبط أكواد الأعضاء والفهرس الفريد دون تكرار.
- المزامنة المالية تستخدم transactions و`ref_type/ref_id` لمنع تكرار العمليات.
- الاستعادة ترفض الملف غير الآمن أو غير السليم أو الناقص، وتنشئ نسخة أمان قبل الاستبدال.

## الصور والكاميرا

- صورة الخيل متاحة مباشرة من ملفه أو المحرر عبر «التقاط بالكاميرا» و«اختيار من المعرض».
- الصور متاحة للعلاج، الغرفة/الإيواء، التحذية، التمارين والمتابعة، الصحة، المواعيد، المصروفات والحجوزات.
- تظهر صور مصغرة في بطاقات الخيول والسجلات وفي الخط الزمني.
- إذن الكاميرا اختياري، وميزة الجهاز معلنة `required=false` حتى لا يُمنع التثبيت على جهاز بلا كاميرا.

## الأمان والبناء

- استعلامات معلّمة، whitelist للجداول والحقول والأنواع، تحقق خدمة من المبالغ والتواريخ والنصوص والملفات.
- `allowBackup=false` و`usesCleartextTraffic=false` وFileProvider للمشاركة.
- لا توجد أسرار أو كلمة مرور توقيع في الكود؛ المفتاح خارج المشروع وكلمة المرور محمية محليًا.
- Release مصغّر عبر R8 وموقّع RSA 4096، ويدعم توقيعي APK v1 وv2.
- اسم التطبيق وفق توجيه المستخدم النهائي هو «سايس الخيل»، والتطبيق مفتوح حاليًا دون مفتاح تفعيل.
