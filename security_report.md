# تقرير الأمان المختصر

- لا توجد مفاتيح تطبيق أو كلمات مرور داخل المصدر؛ نظام مفتاح التشغيل أُلغي بطلب المستخدم.
- توقيع Release في مجلد خارجي محمي، و`key.properties` وملفات JKS مستبعدة من Git.
- قاعدة البيانات تستخدم قوائم سماح للجداول والحقول واستعلامات معلّمة وtransactions.
- التحقق من المدخلات يتم في طبقة قاعدة البيانات، والتحديث الجزئي لا يتجاوز تحقق القيم المقدمة.
- الملفات تُرفض عند مسار غير آمن أو امتداد/ترويسة/حجم غير مطابق، والحد 10 MiB.
- مشاركة الملفات عبر FileProvider، وHTTP الواضح معطّل، ونسخ Android غير الآمن معطّل.
- الأذونات مقتصرة على الكاميرا والإشعارات وما تضيفه مكتبة الإشعارات للاهتزاز.
- صفحة التنزيل بلا تتبع أو مكتبات خارجية، مع CSP ورفض الإطارات وسياسة أذونات مقيدة.
